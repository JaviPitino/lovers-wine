<h2>wines-edit</h2>


<form action="/wines/{{vinoActualizado._id}}/upload" method="POST" enctype="multipart/form-data">


 <label for="nombre">Nombre: </label>
  <input type="text" name="nombre" value="{{vinoActualizado.nombre}}"> 

<br>

  <label for="tipoVino">Tipo Vino: </label>
  <select name="tipoVino" value="{{vinoActualizado.tipoVino}}">
    {{#each tipoVino}}
      <option value="{{ this }}">{{ this }}</option>
    {{/each}}
  </select>

  <br>

  <label for="anada">Añada: </label>
  <select name="anada" value="{{vinoActualizado.anada}}">
    {{#each anada}}
      <option value="{{ this }}">{{ this }}</option>
    {{/each}}
  </select>

  <br>

  <label for="ano">Año: </label>
  <input type="text" name="ano" value="{{vinoActualizado.ano}}">

  <br>

  <label for="denOrigen">Denominación de Origen:  </label>
  <select name="denOrigen" value="{{vinoActualizado.denOrigen}}">
  {{#each denOrigen}}
  <option value="{{ this }}">{{ this }}</option>
  {{/each}}
  </select>

  <br>

  <label for="puntuacion">Puntuación: </label>
  <select name="puntuacion" value="{{vinoActualizado.score}}">
  {{#each score}}
  <option value="{{ this }}">{{ this }}</option>
  {{/each}}
  </select>

  <br>

  <label for="maridaje">Maridaje: </label>
  <select name="maridaje" multiple="true" value="{{vinoActualizado.maridaje}}">
  {{#each maridaje}}
  <option value="{{ this }}">{{ this }}</option>
  {{/each}}
  </select>

<br>

  <label for="image"> Imagen: </label>
  <input type="file" name="image">

<button> <a href="/wines/{{vinoActua._id}}/details"></a> Actulizar Vino </button>

</form>